{% extends "index" %}

{% block content %}

    <h1> {{ entry.title }}</h1>
    
    {{ entry.richText }}
    
    <img src="{{ entry.featureImage.one().getUrl('medium') }}" alt="Feature Image" class="feature-img">

{% endblock %}