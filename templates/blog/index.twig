{% extends "index" %}

{% block content %}

    <h1>Our blog</h1>
    <p>Welcome to blog</p>

    {% set posts = craft.entries.section("newsPosts").all() %}
        {% for post in posts %}
            <h2> {{ post.title }} </h2>

            {{ post.richText|striptags|slice(0, 180) }}... 

            <p> 
                <a href="{{ post.url }}">Read More</a>
            </p>
        {% endfor %}
        
{% endblock %}